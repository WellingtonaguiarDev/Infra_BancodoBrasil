apiVersion: v1
kind: Service
metadata:
  name: svc-machine3otimized
spec:
  selector:
    app: machine3otimized
  ports:
    - protocol: TCP
      port: 8000   
      targetPort: 8000 
  type: ClusterIP
